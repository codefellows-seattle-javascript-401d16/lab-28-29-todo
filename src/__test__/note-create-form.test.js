import React from 'react';
import {shallow} from 'enzyme';
import NoteCreateForm from '../component/note-create-form';

describe('testing note create form', () => {
  test('handle change function should return new state', () => {

  })
})
